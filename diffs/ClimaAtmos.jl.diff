diff --git a/ClimaAtmos.jl/src/parameterized_tendencies/microphysics/microphysics_wrappers.jl b/ClimaAtmos.jl-mod/src/parameterized_tendencies/microphysics/microphysics_wrappers.jl
index c5a7604..c529fd4 100644
--- a/ClimaAtmos.jl/src/parameterized_tendencies/microphysics/microphysics_wrappers.jl
+++ b/ClimaAtmos.jl-mod/src/parameterized_tendencies/microphysics/microphysics_wrappers.jl
@@ -313,6 +313,18 @@ with `dq_lcl_dt`, `dq_icl_dt`, `dq_rai_dt`, `dq_sno_dt` [kg/kg/s].
     q_lcl_hat = eval.λ * shifted_excess
     q_icl_hat = (FT(1) - eval.λ) * shifted_excess
 
+    # Subsaturated, no condensate, no precipitation: every 1-moment process is
+    # identically zero, so this skip is bit-for-bit. The subsaturation test is
+    # load-bearing -- supersaturated air with no condensate still forms cloud.
+    if q_tot_hat <= q_sat_hat &&
+       iszero(q_lcl_hat) &&
+       iszero(q_icl_hat) &&
+       iszero(eval.q_rai) &&
+       iszero(eval.q_sno)
+        z = zero(FT)
+        return (; dq_lcl_dt = z, dq_icl_dt = z, dq_rai_dt = z, dq_sno_dt = z)
+    end
+
     return BMT.bulk_microphysics_tendencies(
         BMT.LinearizedAverage(),
         eval.scheme, eval.mp, eval.tps, eval.ρ, T_hat, q_tot_hat,
