diff --git a/RRTMGP.jl/src/optics/Sources.jl b/RRTMGP.jl-mod/src/optics/Sources.jl
index 8f4c1dc..17e87fc 100644
--- a/RRTMGP.jl/src/optics/Sources.jl
+++ b/RRTMGP.jl-mod/src/optics/Sources.jl
@@ -79,6 +79,12 @@ struct SourceLW2Str{S, D, V, PS} <: AbstractSourceLW
     lev_source::V
     albedo::V
     src::V
+    # Layer coefficients carried from the upward sweep to the downward one.
+    # `lw_2stream_coeffs` is otherwise evaluated twice per layer with identical
+    # arguments, and it contains a sqrt, an exp and an expm1.
+    Rdif::V
+    Tdif::V
+    src_dn::V
 end
 
 function Adapt.adapt_structure(to, src::SourceLW2Str)
@@ -87,6 +93,9 @@ function Adapt.adapt_structure(to, src::SourceLW2Str)
     lev_source = view(leveldata, :, :, 1)
     albedo = view(leveldata, :, :, 2)
     src_view = view(leveldata, :, :, 3)
+    Rdif = view(leveldata, :, :, 4)
+    Tdif = view(leveldata, :, :, 5)
+    src_dn = view(leveldata, :, :, 6)
     return SourceLW2Str{
         typeof(sfc_source),
         typeof(leveldata),
@@ -99,6 +108,9 @@ function Adapt.adapt_structure(to, src::SourceLW2Str)
         lev_source,
         albedo,
         src_view,
+        Rdif,
+        Tdif,
+        src_dn,
     )
 end
 
@@ -107,10 +119,13 @@ function SourceLW2Str(grid_params::RRTMGPGridParams; params::RP.ARP)
     DA = ClimaComms.array_type(grid_params)
     FT = eltype(grid_params)
     sfc_source = DA{FT, 1}(undef, ncol)
-    leveldata = _coalesced_3d(DA, FT, ncol, nlay + 1, 3)
+    leveldata = _coalesced_3d(DA, FT, ncol, nlay + 1, 6)
     lev_source = view(leveldata, :, :, 1)
     albedo = view(leveldata, :, :, 2)
     src = view(leveldata, :, :, 3)
+    Rdif = view(leveldata, :, :, 4)
+    Tdif = view(leveldata, :, :, 5)
+    src_dn = view(leveldata, :, :, 6)
 
     return SourceLW2Str{
         typeof(sfc_source),
@@ -124,6 +139,9 @@ function SourceLW2Str(grid_params::RRTMGPGridParams; params::RP.ARP)
         lev_source,
         albedo,
         src,
+        Rdif,
+        Tdif,
+        src_dn,
     )
 end
 
diff --git a/RRTMGP.jl/src/rte/longwave_2stream.jl b/RRTMGP.jl-mod/src/rte/longwave_2stream.jl
index ffbd1b4..e44a648 100644
--- a/RRTMGP.jl/src/rte/longwave_2stream.jl
+++ b/RRTMGP.jl-mod/src/rte/longwave_2stream.jl
@@ -257,6 +257,8 @@ Equations are after Shonk and Hogan 2008, doi:10.1175/2007JCLI1940.1 (SH08)
     (; flux_up, flux_dn) = flux
 
     (; albedo, lev_source, sfc_source, src) = src_lw
+    # Layer coefficients, computed in the upward sweep and reused below.
+    Rdif_c, Tdif_c, src_dn_c = src_lw.Rdif, src_lw.Tdif, src_lw.src_dn
     (; inc_flux, sfc_emis) = bcs_lw
     FT = eltype(τ)
     @inbounds flux_dn_ilevplus1 =
@@ -293,6 +295,12 @@ Equations are after Shonk and Hogan 2008, doi:10.1175/2007JCLI1940.1 (SH08)
             src_up + Tdif * denom * (src_ilev + albedo_ilev * src_dn)
         albedo[gcol, ilev + 1], src[gcol, ilev + 1] =
             albedo_ilevplus1, src_ilevplus1
+        # The downward sweep needs exactly these, for this layer, from the same
+        # arguments. Storing three coalesced values costs less than a second
+        # sqrt/exp/expm1 per layer.
+        Rdif_c[gcol, ilev] = Rdif
+        Tdif_c[gcol, ilev] = Tdif
+        src_dn_c[gcol, ilev] = src_dn
         lev_src_bot = lev_src_top
         albedo_ilev, src_ilev = albedo_ilevplus1, src_ilevplus1
     end
@@ -303,20 +311,11 @@ Equations are after Shonk and Hogan 2008, doi:10.1175/2007JCLI1940.1 (SH08)
         src[gcol, nlev]                          # scattering by the direct beam below
 
     # From the top of the atmosphere downward -- compute fluxes
-    @inbounds lev_src_top = lev_source[gcol, nlay + 1]
     ilev = nlay
     @inbounds while ilev ≥ 1
-        lev_src_bot, albedo_ilev, src_ilev =
-            lev_source[gcol, ilev], albedo[gcol, ilev], src[gcol, ilev]
-        τ_lay, ssa_lay, g_lay =
-            τ[gcol, ilev], ssa[gcol, ilev], g[gcol, ilev]
-        Rdif, Tdif, _, src_dn = lw_2stream_coeffs(
-            τ_lay,
-            ssa_lay,
-            g_lay,
-            lev_src_bot,
-            lev_src_top,
-        )
+        albedo_ilev, src_ilev = albedo[gcol, ilev], src[gcol, ilev]
+        Rdif, Tdif, src_dn =
+            Rdif_c[gcol, ilev], Tdif_c[gcol, ilev], src_dn_c[gcol, ilev]
 
         denom = FT(1) / (FT(1) - Rdif * albedo_ilev)  # Eq 10
         flux_dn_ilev =
@@ -328,7 +327,6 @@ Equations are after Shonk and Hogan 2008, doi:10.1175/2007JCLI1940.1 (SH08)
             src_ilev
         flux_dn[gcol, ilev] = flux_dn_ilev
         flux_dn_ilevplus1 = flux_dn_ilev
-        lev_src_top = lev_src_bot
         ilev -= 1
     end
 end
