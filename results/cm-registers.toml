note = "params passed as runtime kernel arguments; as const globals the compiler folds their fields and reports 101 instead of 163 for LinearizedAverage"

[layers."+ aggregate (Instantaneous)"]
local_bytes = 32
registers = 62
warps_per_sm = 16

[layers."LinearizedAverage nsub=1"]
local_bytes = 32
registers = 207
warps_per_sm = 8

[layers."LinearizedAverage nsub=2"]
local_bytes = 32
registers = 207
warps_per_sm = 8

[layers."LinearizedAverage nsub=3"]
local_bytes = 32
registers = 207
warps_per_sm = 8

[layers.launch_bounds_probe]
spill_growth_bytes = 360

    [layers.launch_bounds_probe.bounded_12warps]
    local_bytes = 1120
    registers = 168
    warps_per_sm = 12

    [layers.launch_bounds_probe.unbounded]
    local_bytes = 760
    registers = 255
    warps_per_sm = 8

    [layers.launch_bounds_probe.unbounded_no_always_inline]
    local_bytes = 856
    registers = 255
    warps_per_sm = 8

[layers."one linearized implicit step"]
local_bytes = 32
registers = 76
warps_per_sm = 16

[layers."source terms only"]
local_bytes = 32
registers = 52
warps_per_sm = 16
